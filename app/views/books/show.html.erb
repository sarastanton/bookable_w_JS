<h1> <%= @book.title %> by <%= @book.author.name %> </h1>

<br />

<%= button_to "Edit Book Details", edit_book_path(@book), :method => :get %>
<br />


<h4> Genre: </h4> <%= @book.genre.name %>

<br />
<br />

<h4> Page count: </h4> <%= @book.page_count %>

<br />
<br />

<h4> Average rating: </h4> <%= @book.average_rating %>

<br />
<br />

<h4> User Reviews: </h4> <%= @book.review_count %>

<br />
<br />

<%= button_to "Show Reviews", book_reviews_path(@book), {:method => :get, form_class: "show_reviews_button", class: "show_reviews_button"} %>

<br />

<div id="show-reviews">
  <ul>
  </ul>
</div>

<br />

<h2> Add a review for <%= @book.title %>: </h2>
<br />
<p> <strong> Note: </strong> This will also add <%= @book.title %> to your profile under "Books I Have Read" if it's not already there (because how else could you review it?!) </p>
<br />
<%= render partial: "reviews/review_form", locals: {review: @review, book: @book} %>

<br />

<%= render partial: "layouts/errors", locals: {object: @review} %>

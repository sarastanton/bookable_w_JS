<%= javascript_include_tag 'ratings' %>


<h5> How would you rate <%= @book.title %> by <%= @book.author.name %>?</h5>

<%= render partial: "rating_form", locals: {book: @book, rating: @rating} %>

<%= render partial: "layouts/errors", locals: {object: @rating} %>
